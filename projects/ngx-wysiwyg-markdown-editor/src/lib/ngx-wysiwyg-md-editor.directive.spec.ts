import { WysiwygMdEditorDirective } from './wysiwyg-md-editor.directive';

describe('WysiwygMdEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new WysiwygMdEditorDirective();
    expect(directive).toBeTruthy();
  });
});
